#include<stdio.h>

void remove_double(int last_digit, int date)
{
    for(int i = last_digit-1; i > -1; i--)
    {
        if(date[last_digit] == date[i])
        {

        }
    }
}

int main()
{
    int input = 0;
    scanf("%d", input);
    char input_string[] = ()
    int last_digit;
    for(int i = 0; i < 5; i++)
    {
        if(input[i] == '.')
        {
            last_digit = i-1;
            break;
        }
    }

}